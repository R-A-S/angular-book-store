export interface Book {
  id: number;
  cover: string;
  title: string;
  author: string;
  price: number;
}

export interface User {
  username: string;
  token: string;
  avatar: string;
}
